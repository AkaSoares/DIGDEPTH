/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor


if (image_index == 1) {
    // Verifique se não há nenhuma instância de o_cube
    if (!instance_exists(o_cube)) {
        // Se não houver, crie uma instância de o_cube nas coordenadas (390, 20)
        instance_create_layer(-80, -80, "Above", o_cube);
    }
}
	













